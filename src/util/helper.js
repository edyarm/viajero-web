const Helper = {
    weekDays: ['Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday'],
    kelvinToFahrenheit: k => ((k - 273.15) * 9 / 5 + 32).toFixed(0),
}

export default Helper;